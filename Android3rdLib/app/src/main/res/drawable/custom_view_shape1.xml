<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- shape = "rectangle" 时使用-->
    <corners
        android:radius="2dp"
        android:topLeftRadius="3dp"/>

    <!-- 渐变 -->
    <gradient
        android:startColor="#ff0000"
        android:endColor="#ffffff"
        android:centerColor="#00FF00"
        android:gradientRadius="10dp"
        android:centerX="0.5"
        android:centerY="0.5"
        android:type="sweep"
        />

    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp"/>
    <size
        android:width="20dp"
        android:height="20dp"/>

    <!-- 填充颜色 -->
    <!--<solid
        android:color="#FF00FF"/>-->

    <!--指定边框-->
    <stroke
        android:width="5dp"
        android:dashWidth="2dp"
        android:dashGap="1dp"
        android:color="#00FFFF"/>

    <!--虚线宽度  android:dashWidth
        虚线间隔宽度 android:dashGap-->

</shape>